{
  "name": "lewis-crm-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Scoped MCP adapter over the Lewis Insurance Supabase (jarvis-os) for the Mac Mini Hermes office agents. Thin layer over the REAL tables + existing rails — never a new backend.",
  "module": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.7.0",
    "@supabase/supabase-js": "^2.45.0",
    "pdf-lib": "^1.17.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
